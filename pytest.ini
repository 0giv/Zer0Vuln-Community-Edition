[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts = -ra --strict-markers --tb=short
markers =
    integration: tests that hit a real MySQL/RabbitMQ — skipped by default in CI
